# I Need Money

> Work in progress

## Run

```shell
mvn install
mvn compile
```

### Rest API application

```shell
mvn clean springboot:run -cp api
```

### Desktop application

```shell
(cd desktop && mvn clean javafx:run)
```
