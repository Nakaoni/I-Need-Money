# I Need Money

> Work in progress

## Run

```shell
mvn install
mvn compile
```

### Rest API application

```shell
(cd api && mvn clean spring-boot:run)
```

### Desktop application

```shell
(cd desktop && mvn clean javafx:run)
```
